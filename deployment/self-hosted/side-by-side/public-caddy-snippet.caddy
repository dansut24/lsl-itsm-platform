itsm.hi5tech.co.uk {
    encode zstd gzip

    handle /healthz {
        reverse_proxy hi5self-web:80
    }

    handle {
        reverse_proxy hi5self-web:80
    }

    header {
        X-Content-Type-Options "nosniff"
        Referrer-Policy "strict-origin-when-cross-origin"
        X-Frame-Options "SAMEORIGIN"
        -Server
    }
}
