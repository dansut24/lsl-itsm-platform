// RMM estate/runtime data must come from the tenant RMM service. These exports
// remain as empty compatibility interfaces while the legacy RMM shell is retired.
export const rmmSites = Object.freeze([])
export const rmmDeviceGroups = Object.freeze([])
export const rmmDevices = Object.freeze([])
export const rmmAlerts = Object.freeze([])
export const rmmPatchGroups = Object.freeze([])
export const rmmJobs = Object.freeze([])
export const rmmSoftware = Object.freeze([])
export const rmmScripts = Object.freeze([])
export const rmmPolicies = Object.freeze([])
export const rmmActivity = Object.freeze([])
